# Master Plan — Block II Methodology

The durable methodology behind Block II. Set structure is adjustable per block:
the default is one warm-up plus two working sets in a rep range; some blocks are
taken to failure (e.g. Thursday dips). Rep ranges live on the exercises, not the
block scheme. Block + bank: each block is a bank — pick one option per session.
