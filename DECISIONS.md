# Decisions Ledger — Block II / Adjustable-Sets redesign

Append-only. Every implementer brief copies this file in full. Seeded from the 2026-09-20 scoping interview with Marcus.

D1: Community model = **Creator-Program** ("Push On by Michael Smith" / Playbook style — self-serve subscribe + load Marcus's published plan + community check-in feed), NOT the coach→named-client CRM in the old REDESIGN_BRIEF §8. — Marcus rejected the CRM (it's the archived-coaching direction); creator-program compounds with his content. — Binds: any community work (B4, OUT of scope this run) builds the creator-program model; §8-REVISED in REDESIGN_BRIEF §0 is authoritative. — scope, 2026-09-20
D2: Community wants "same as Michael Smith but with slight adjustments to make it his own" — not a bar-for-bar copy. — Marcus's words. — Binds: B4 design (out of scope this run) — note for later. — scope, 2026-09-20
D3: Aesthetic = **Minimalist mono** (near-monochrome, tiny type scale, dense-but-clean, one hairline accent, function-first). Evolve the existing dark-mono foundation, don't reinvent an identity. — Marcus picked it from 4 options. — Binds: the reskin (design-led, SEPARATE frontend-engineer pass — OUT of scope this autonomous run). — scope, 2026-09-20
D4: **Set structure is adjustable per plan/block** — replace the hardcoded "failure block = fixed WU+W1+W2 (3 sets)" with a configurable set-scheme (working_sets:int, rep_min/rep_max OR to_failure:bool, optional warm-up count). — Marcus: "I want the 1WU+2-sets-to-failure to be adjustable depending on plan." His new Block II is written as "2 working sets × rep-range." — Binds: B1 (this is the core change); B3 seeds against this model; must stay additive to the wiring contract (REDESIGN_BRIEF §9 — set_logs/pr_history append-only, weight_kg storage, no renamed columns/enums/mutation signatures). — B1, 2026-09-20
D5: **Cut the hard/soft validation guardrails** (48h recovery, push/pull balance, min rest day, compound-recovery window, cardio-order) — remove or make fully optional + OFF by default. — Marcus did not keep them when asked what to keep; "simplify mechanisms." — Binds: B2; simplify plan/day editors accordingly; do NOT touch block+bank, macro/nutrition, or PR detection. — B2, 2026-09-20
D6: **KEEP** — block+bank fresh-choice model, the WU+working-sets protocol (now adjustable per D4), full macro/nutrition + AI photo estimator, append-only PR detection. — Confirmed kept in the interview. — Binds: all slices — these are not to be removed or degraded. — scope, 2026-09-20
D7: This autonomous run = **B1 (adjustable set-scheme) + B2 (cut validation) + B3 (seed Block II)** ONLY. The mono reskin and the community layer are explicitly OUT of scope this run. — Marcus greenlit an autonomous backend/logic run while he does GRE + filming; reskin is design-led (his "backend-first, separate design session" doctrine); community held to post-Nov-16. — Binds: Scope-Gate — reject scope creep into reskin/community. — scope, 2026-09-20
D8: Block II seed source of truth = `spec/BLOCK_II_SEED.md` (transcribed from Marcus's handwritten plan, photos in second_brain/raw/physical/documents/2026-09-20-\*). — Do not re-read handwriting; the transcription is authoritative. — Binds: B3. — B3, 2026-09-20
D9: Set-scheme lives as additive columns on `blocks` (`warmup_sets` default 1, `working_sets` default 2, `to_failure` default false); defaults reproduce today's WU+W1+W2 (3-set) behavior so un-migrated/legacy blocks are unchanged. `isBlockComplete` auto-completes at `warmup_sets+working_sets` instead of hardcoded 3. No `set_role` column is added — role stays derived from `set_index` position. — Keeps the wiring contract (set_logs append-only, set_index int) intact while making structure configurable; back-compat default means no data migration of existing logs. — Binds B1 (build) + B3 (seeds these columns). — B1, 2026-09-20
D10: Runtime methodology guardrails are minimal — only `weekRestDayError` (hard) + cardio-order (soft) run in the editor; the rich rules (48h recovery, push/pull) run ONLY at seed time as non-blocking warnings. B2 downgrades the min-rest-day HARD rule to non-blocking so a no-rest-day plan (Block II) saves + seeds. The unrelated data-integrity guards in plan/mutations.ts (no-remove-logged-session, blocks-frozen) are NOT touched. — Discovered in the 2026-09-20 seam survey; corrects the assumption that guardrails block runtime broadly. — Binds B2 + B3 (Block II has no full rest day). — B2, 2026-09-20
D11: `to_failure` column: keep DEFAULT false for NEW blocks (D4 wants opt-out) BUT backfill existing failure blocks in migration 025 — `UPDATE blocks SET to_failure = true WHERE block_type = 'failure';`. — A block_type='failure' block meant "last set to failure"; without the backfill every legacy block silently logs is_to_failure=false, corrupting the history "F" badge + progression signal (all 3 councils, confirmed must-fix). — Binds B1-fix + B3 (seed sets to_failure explicitly per block, e.g. Thu dips=true). — B1-fix, 2026-09-20
D12: Scheme columns are conceptually lifting-only. Scope migration 025's CHECK so working_sets>=1 binds only lifting/failure blocks (mirror the existing `blocks_lifting_has_type` partial-CHECK pattern), so cardio/recovery/mobility rows don't carry a meaningless mandatory working_set. — Maintainer finding; avoids dead misleading constraints. — Binds B1-fix migration. — B1-fix, 2026-09-20
D13: The scheme write path lives in `src/lib/library/mutations.ts` createBlock/updateBlock (+ BlockPayload/schema + minimal block-form inputs). On insert default `to_failure = (block_type === 'failure')`; on updateBlock, when block_type flips, reconcile to_failure to match unless the form overrides. — Bug Hunter F2/F3: without a write path the "adjustable" feature is inert and block_type/to_failure drift into self-contradiction. Minimal functional form inputs only (NOT a restyle — reskin stays out of scope). — Binds B1-fix. — B1-fix, 2026-09-20
D14: `src/lib/supabase/types.ts` was HAND-EDITED (no live DB in the worktree). This is a pre-push stopgap: migration 025 must be `supabase db push`ed BEFORE any `supabase gen types` regen, or the regen deletes the three columns → blockSetCount returns NaN → failure blocks never auto-complete (the migration-023 lockout class). Recorded in KNOWN_ISSUES.md. — Maintainer F2. — Binds anyone regenerating types. — B1-fix, 2026-09-20
D15: The seed plan markdown (`supabase/seed/wiki/*.md`) is GITIGNORED — a local mirror of the vault, no example in-repo. B3 authors `supabase/seed/wiki/current-plan.md` (+ minimal companion files readWikiFiles requires) for Block II by reverse-engineering the grammar from `supabase/seed/lib/parser.ts` + `parsePlanFromWiki` in `methodology-rules.ts`. Block set-scheme is expressed in the markdown via the plan's own "N × reps" / "N × failure" notation → parser extended to emit working_sets + to_failure on ParsedBlock; rep ranges stay on EXERCISES (prescribed_min/max), NOT the block scheme. Most Block II lifts are block_type='failure' (structured) with to_failure=FALSE; only Thursday dips = to_failure=true (2×failure). Verification is parser-in-isolation (no live Supabase): assert the parsed TrainingPlanSpec + validateTrainingPlan hardViolations empty; the syncGlobalBlocks scheme-write is verified by code-reading. — Binds B3. Committed artifact = parser/sync extensions + verify script (the .md is local/gitignored). — B3, 2026-09-20
--- Mobile IA Rewire (redesign phase 2) — D17+ govern this build ---

D17: This build = **Mobile IA Rewire** — turn the approved `/demo` phone-frame designs into the real mobile app, wired end-to-end to the EXISTING Supabase backend + data layers (which are all built). It is IA-restructure + reskin-to-`/demo` + wiring, NOT new backend. In scope: the 5 daily surfaces (Today, Logger, Nutrition, Plan, Progress) fully wired + reskinned; new 5-tab nav; owner-gating of authoring. — Resolved in the 2026-09-22 collaborative design session; Marcus: "lets start building the mobile design and make sure it works end to end while leaving wiring open for desktop builder." — Binds: all slices; Scope-Gate rejects creep into the admin hub / community backend. — scope, 2026-09-22

D18: **Authoring split = desktop-only builder.** Mobile is the clean daily viewer/logger; ALL plan/program authoring (add exercises, create/edit blocks, create/edit programs, plan/day editing, publish) is owner-only and lives in a future desktop admin hub. — Marcus locked this (AskUserQuestion, "Desktop-only builder"); it's the single biggest simplifier for a clean mobile app. — Binds: S0 owner-gate; Library + plan/day-edit routes become owner-only; mobile hides all edit affordances. — scope, 2026-09-22

D19: **New mobile IA (5 tabs):** Today · Plan · Nutrition · Progress · Community. Settings = gear, not a tab. Removed as tabs: **Library** (→ owner-only), **Trends + History** (→ MERGED into the Progress tab as segmented Overview/By-exercise/History). "Fuel" renamed to **Nutrition** (Marcus's call). Community = its own 5th tab (headline feature deserves top-level). — Falls out of D18 + Marcus's "cut tabs / decrease functionality" ask + the design session. — Binds: S0 (nav), S5 (Progress merge), S6 (Community). — scope, 2026-09-22

D20: **Monetization = 3 tiers:** (1) free app (log your own training), (2) paid subscription to train with Marcus (load his published plans + community, ~$15/mo), (3) FUTURE 1-on-1 coaching **inquiry funnel** (lead capture → Marcus follows up; NOT the rejected coach→client CRM). Launch **free-first** — paywall UI wired but access free; Stripe deferred behind the same button. — Marcus: "I do like how the app is free and then subscription to train with me… add a button for 1-on-1 coaching inquiry (future build)." — Binds: S6 Community placeholder shows the tiers; coaching button = FUTURE_WORK #11; Stripe = future. — scope, 2026-09-22

D21: **Community scope THIS build = navigable placeholder** ("coming soon" shell in the `/demo` language). Reason: nothing can publish programs until the desktop admin hub exists, so a "working" storefront has nothing in it. Full Community (storefront, subscribe, load, feed) builds together WITH the admin hub as the next build, on real data. — Marcus (AskUserQuestion, "Navigable placeholder now"). — Binds: S6; do NOT build the subscription/feed backend this run. — scope, 2026-09-22

D22: **Owner-gating mechanism** = a server-side `isOwner(userId)` in `src/lib/auth/owner.ts` checking an env-listed `OWNER_USER_IDS` (simplest; no schema change) — a `profiles.is_owner` flag is the alternative if a DB source is preferred. Non-owners hitting an authoring route get `notFound()`/redirect to `/today`. — Chosen for simplicity over a migration; the owner is effectively just Marcus for now. — Binds: S0 + every authoring route (library, plan/day edit, /admin). — S0, 2026-09-22

D23: **Admin-hub analytics spec captured** at `spec/ADMIN_HUB_ANALYTICS.md` (research 2026-09-22). North Star = **Weekly Active Loggers**; aha = first full workout; **8-tile v1 dashboard computable from existing Supabase tables** (SQL views, $0), PostHog free tier only at v1.5 for funnels/replay; two visuals deserve craft — cohort retention heatmap + program subscribers×adherence leaderboard. — Marcus asked to "research what stats/data/visualizations ensure an app like this runs healthily." — Binds: the FUTURE desktop admin-hub build (not this one). — scope, 2026-09-22

D24: **Owner-gate is UI-only THIS build; data-boundary enforcement DEFERS to the community/admin-hub build.** S0's `requireOwner()` gates the authoring PAGES (`/library/**`, `/plan/edit`, `/plan/[day]/edit`) but NOT the write path: library/plan mutations are client-side `BrowserClient` calls under per-user RLS (`auth.uid() = owner_user_id`), so an authenticated non-owner can still author THEIR OWN private silo by calling a mutation directly. This is NOT a cross-user breach (RLS silos every user; nobody can touch Marcus's data) and is NOT a regression (the app always let each user author their own library). It is an UNENFORCED PRODUCT RULE ("followers don't author"), and there are NO followers this build (Community is a placeholder, D21). — Roast S0 Security F1/F2 (CONFIRMED); Judge triaged defer-not-must-fix because no followers exist + no cross-user exposure + enforcement design depends on the not-yet-built follower/subscription model. — Binds: the FUTURE community/admin-hub build MUST enforce owner-only authoring at the DATA boundary — convert authoring mutations to server actions/route handlers that `await requireOwner()`, and/or add an owner-allowlist check into the INSERT/UPDATE/DELETE RLS policies on blocks/exercises/workout_defs/cardio_activities/daily_schedules/training_plans. Recorded in KNOWN_ISSUES.md. — S0-roast, 2026-09-22

D25: **Adopt an `(owner)/` route group for authoring when `/admin` is built (S6).** Plan authoring is currently guarded per-page (`await requireOwner()` in `/plan/edit` + `/plan/[day]/edit`) because a `plan/layout.tsx` guard would also block the follower-viewable plan pages. That leaves coverage dependent on per-page discipline with no structural guarantee, and the planned `/admin` hub has no guard yet. — Roast S0 Security F3 (hardening). — Binds: S6 — move `/plan/edit`, `/plan/[day]/edit`, `/admin` under a `(app)/(owner)/` route group with ONE `layout.tsx` calling `requireOwner()`, and add a coverage test asserting every `(owner)/` path 404s for a non-owner. Library is already structurally covered by `library/layout.tsx`. — S0-roast, 2026-09-22

D16: REVISE B1's completion model to match Marcus's real training (2026-09-22). `working_sets` becomes a SOFT TARGET, not a hard auto-complete cap: the logger renders warm-up + target working slots, but ALWAYS offers "+ Add working set" to exceed the target (W3, W4…), and a scheme block completes ONLY when the user taps "Complete block" (manual, via completedBlockIds) — it NEVER auto-locks at the count. `to_failure` stays the optional per-set is_to_failure toggle (SetEntryForm) + drives whether the failure checkbox defaults on working sets; it is NOT a completion concept. — Marcus: "two working sets per exercise but sometimes 3 maybe 4 — depends on the plan or what I'm feeling that day." — Binds: isBlockComplete (drop the auto-complete-at-count for 'failure' blocks → manual like free-form), FailureProtocol (add-set affordance + manual complete), verify-set-scheme tests (update to the target/manual model). Reskin decisions also locked: light theme + Satoshi + semantic colors (success #22C55E, warning #EAB308 bright yellow, danger #DC2626, cardio #14B8A6 bright teal). — B-flex, 2026-09-22

--- Tier 2 (post-validation build) — D26+ ---

D26: **Exercise catalog = clean-license.** Use `yuhonas/free-exercise-db` (public domain / Unlicense, media included) as the catalog source, NOT `hasaneyldrm/exercises-dataset`. Reason: the hasaneyldrm media (per-exercise GIF animations) is **© Gym Visual**, included "with permission for that repo only" — the data is MIT but the media is NOT redistributable, and the maintainer has an unmerged commit stripping it "due to ownership disputes." Marcus's roadmap is a PAID community where followers load his programs = commercial redistribution. free-exercise-db is public domain AND carries `mechanic` (compound/isolation) + `force` + primary/secondary muscles — which also supplies the compound classification. Trade-off accepted: static start/end photos instead of animations. — Marcus 2026-09-23 ("can i do clean-license for now? and i can always sub in videos and animations that i either find or create myself"). — Binds T2-A. — scope, 2026-09-23

D27: **Exercise media is a PLUGGABLE field.** Add `media_path` (same-origin, e.g. `/exercises/<slug>.jpg|mp4`) + `media_type` ('image'|'gif'|'video') to `exercises`. Media is vendored into `public/exercises/` (same-origin — the CSP blocks external hosts, so no CDN/Storage for this). Swapping in Marcus's own filmed clips later = drop the file in + update that row's path/type. No schema or code change required to upgrade any single exercise. — Marcus wants to sub in his own videos/animations over time. — Binds T2-A and any future media work. — scope, 2026-09-23

D28: **e1RM compound list (locked).** Gets estimated-1RM + the weight-PR trend: Bench Press · Incline Press · Overhead/Shoulder Press · Back Squat · Deadlift (RDL/Sumo/conventional) · Barbell/DB/Chest-Supported Row · **Weighted Dips** · Weighted Pull-up/Chin · **Lat Pulldown**. Everything else (lateral raise, rear delt, biceps, triceps, chest fly, face pull, calf raise, walking lunges, single-arm cables, core) = isolation, NO e1RM. NOTE: `exercises.is_compound` ALREADY exists and `buildE1rmSpotlights` ALREADY excludes non-compound + bodyweight — so the code gate exists; the DATA is what's wrong (rows sitting on the `false` default). T2-A fixes the data + verifies the gate holds on every e1RM surface (incl. the per-exercise detail page). — Marcus set the list 2026-09-23. — Binds T2-A/T2-B. — scope, 2026-09-23

D29: **Body tracking lives in Progress, not Settings.** Bodyweight logging AND progress-photo capture both live in a "Body" area of the Progress tab alongside their charts/timeline — log where you see the trend. Settings stays config-only. — Marcus chose this over Settings-input/Progress-display. — Binds T2-C. — scope, 2026-09-23

D30: **PR type colors (locked):** weight PR = blue `#2563EB` (`--pr-weight`), in_range_rep PR = violet `#7C3AED` (`--pr-rep`). Single source of truth `src/lib/methodology/pr-colors.ts`; the per-exercise PR graph reads its legend from that map. — Marcus chose blue/violet. — Binds T2-B + all PR surfaces. — validation round 1, 2026-09-23

D31: **Estimated 1RM (Epley) is DELETED as a product concept.** Replaced by the per-exercise **PR history chart** — real logged weight PRs (blue, left axis) + rep PRs (violet, right axis) from append-only `pr_history`. Reason: e1RM is a derived guess; Marcus wants only real data. Consequence: `is_compound` no longer gates any chart (it was introduced in T2-A to keep e1RM off isolations). The flag STAYS as editable Library metadata + an input to the future builder — do not re-attach display logic to it without a decision. — Marcus 2026-09-23 ("we can just eliminate estimated one rep max and just do best reps and progression for all exercises"). — Binds T2-B onward. — scope, 2026-09-23

D32: **Every slice ends with a real browser drive (human-like) and the build only continues if it passes.** Static gates (format/typecheck/lint/build) and fixture tests are NOT sufficient to call a slice done — they were all green while a Progress segmented control never switched, a goal-mode control was built from dead `<span>`s, and the PR chart hid one of its two series. Reusable harness lives at `e2e/harness/` (`startSession` injects Supabase SSR cookies for a throwaway `@trainingapp.test` user — never Marcus's account — seeds data, returns a Playwright page; `stop()` always cleans up in a finally). Each slice gets an `e2e/drive-<slice>.mjs` that INTERACTS (click/type/submit) and RELOADS to prove persistence, captures console errors, and screenshots both viewports — and the screenshots must be LOOKED AT, not just asserted on. A failing drive blocks the slice. — Marcus 2026-09-23 ("set up your own debugging by opening a browser that lets you test the mechanisms as if you are a human - embed that into the end of every slice"). — Binds every future slice. — process, 2026-09-23

D33: **Progress → Trends PR history shows ONE exercise at a time, behind a selector.** The multi-spotlight list is gone: `buildPRSpotlights` now feeds a `<select>` (native, token-styled, 44px tall, `PR_SPOTLIGHT_LIMIT` raised 4 → **12**) and exactly one exercise's two charts render below it. The **Overview** featured slot is unchanged — one featured lift, no control, because Overview is the glance. — Marcus 2026-09-24 ("maybe we can do a selector to select which exercise to surface and only surface one - so you can select which exercise and see trends"); resolves the T2-D hand-off flag that 4 exercises × 2 charts = 8 canvases defeated the scannability the reorder was for. A native select over the shared SegmentedControl because the option list is DATA: a dozen equal-width pills either overflow 390px or compress to slivers, whereas the OS picker is the best one-handed long list a phone has, cannot overflow, and is keyboard/AT-correct for free. Server-first: the page builds every selectable exercise's model and passes them all down; the client component only chooses which renders (exactly one is mounted — nothing to hide, so the `hidden`-vs-`.flex` class of bug cannot recur here). — Binds T2-E and any future per-exercise Progress surface. — T2-E, 2026-09-24

D34: **A type label is never a note.** "Compound" / "Isolation" / "Accessory" / "Failure" / "Mobility" / "Corrective" belong to `exercises.is_compound` (D28) and `blocks.block_type`, NOT to `exercises.notes` — the free-text field the logger's exercise picker prints under every option. The predicate is `isTypeOnlyNote` in `src/lib/methodology/exercise-notes.ts` (pure, fixture-tested): a note is clearable only when EVERY non-empty line is exactly a type word after trimming, case-insensitive. Deliberately strict — "Compound — PR", "Isolation (ATG day)" and "Lower Compound day" are REAL notes and are kept, as is "Compound." (punctuation is not stripped; stripping punctuation is the first step toward stripping meaning). Data fix = `scripts/clean-exercise-notes.ts` (dry-run default, `--apply`, `--user` REQUIRED, scoped writes, idempotent). Source fix = the seed parser drops a type-only Notes cell at the point of reading, and BOTH merge points (`methodology-rules.ts` per-block, `seed-from-wiki.ts` `collectExercises`) now de-duplicate lines via `mergeExerciseNotes` — the old joins concatenated identical notes once per round, which is what produced "Compound\nCompound\nCompound". Guarded against re-seed by two assertions in `scripts/verify-block-ii-parse.ts`. — Marcus 2026-09-24 (39 of his 83 exercises carried the junk; 26 real notes had to survive). — Binds T2-E and the seed. — T2-E, 2026-09-24
